input {
  beats {
    port => 5044
  }
}

output {
  stdout {
    codec => rubydebug
  }
}

input {
  udp {
    port  => 2055
    codec => netflow
  }
}

output {  
    elasticsearch {
    hosts => ["e2nwelasticsearch:9200"]
    index => "e2nw-netflow-%{+YYYY.MM.dd}" }
}
